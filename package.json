{
  "name": "estemp-backend",
  "version": "1.0.0",
  "description": "Node.js Express server with MongoDB EStamp model",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix client install && npm --prefix client run build && node -e \"const fs=require('fs'); if(!fs.existsSync('dist')){fs.cpSync('client/dist','dist',{recursive:true})}\"",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "mongoose": "^8.24.4"
  }
}
